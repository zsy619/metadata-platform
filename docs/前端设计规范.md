# 元数据平台 - 前端设计规范 (Design System) v2.1

> **版本**: 2.1 (完整配色集成版)  
> **更新日期**: 2026-01-25  
> **基准配色**: 经典企业蓝 (Indigo #4051B5 / Orange #FF9800)  
> **技术栈**: Vue 3 + Element Plus + Native HTML  
> **目标**: 统一企业级应用视觉语言，建立完整的前端组件标准库。

---

## 🏗 全局基础 (Global Foundation)

### 1. 色彩体系 (Colors)

基于 CSS 变量的全局色彩管理，支持未来主题切换。

```css
:root {
  /* ========== 品牌主色 (Brand Primary) ========== */
  --el-color-primary: #4051B5;        /* INDIGO: 核心交互、按钮、导航 */
  --el-color-primary-light-3: #7986cb;
  --el-color-primary-light-5: #9fa8da;
  --el-color-primary-light-7: #c5cae9;
  --el-color-primary-light-8: #e8eaf6;
  --el-color-primary-light-9: #f5f6fa;
  --el-color-primary-dark-2: #303F9F;

  /* ========== 品牌点缀 (Accent) ========== */
  --el-color-accent: #FF9800;         /* ORANGE: 品牌识别、Logo、VIP、高亮 */
  --el-color-accent-light: #FFB74D;
  --el-color-accent-dark: #F57C00;

  /* ========== 功能色 (Functional) ========== */
  --el-color-success: #67C23A;        /* 成功/通过/启用 */
  --el-color-success-light: #e1f3d8;
  --el-color-warning: #E6A23C;        /* 警告/注意 */
  --el-color-warning-light: #fdf6ec;
  --el-color-danger: #F56C6C;         /* 错误/危险/删除 */
  --el-color-danger-light: #fef0f0;
  --el-color-info: #909399;           /* 信息/未开始/禁用 */
  --el-color-info-light: #f4f4f5;

  /* ========== 结构背景 (Structure) ========== */
  --el-bg-color-page: #F0F2F5;        /* 物理底层背景 (桌面) */
  --el-bg-color: #FFFFFF;             /* 内容承载层 (纸张/卡片) */
  --el-bg-color-overlay: #FFFFFF;     /* 浮层背景 (弹窗/抽屉) */
  --el-fill-color-light: #F5F7FA;     /* 表头/禁用背景 */
  --el-fill-color-lighter: #FAFAFA;   /* 悬停背景 */
  --el-fill-color-extra-light: #FAFCFF; /* 选中背景 */

  /* ========== 文字体系 (Typography) ========== */
  --el-text-color-primary: #303133;   /* 主要文字 (标题, 正文) */
  --el-text-color-regular: #606266;   /* 常规文字 (标签, 列表) */
  --el-text-color-secondary: #909399; /* 次要文字 (说明, 辅助) */
  --el-text-color-placeholder: #C0C4CC; /* 占位符 */
  --el-text-color-disabled: #C0C4CC;  /* 禁用文字 */

  /* ========== 边框 (Borders) ========== */
  --el-border-color: #DCDFE6;
  --el-border-color-light: #E4E7ED;
  --el-border-color-lighter: #EBEEF5;
  --el-border-color-extra-light: #F2F6FC;
  
  /* ========== 圆角 (Radius) ========== */
  --el-border-radius-base: 4px;       /* 按钮、输入框、标签 */
  --el-border-radius-small: 2px;      /* 徽标 */
  --el-border-radius-round: 20px;     /* 圆角按钮 */
  --el-border-radius-circle: 100%;    /* 圆形头像 */
  
  /* ========== 阴影层级 (Shadows) ========== */
  --el-box-shadow-light: 0 2px 12px 0 rgba(0, 0, 0, 0.05);  /* 下拉菜单、气泡 */
  --el-box-shadow: 0 4px 16px 0 rgba(0, 0, 0, 0.08);        /* 卡片默认 */
  --el-box-shadow-dark: 0 8px 24px 0 rgba(0, 0, 0, 0.12);   /* 弹窗/抽屉 */
}
```

### 2. 布局与间距 (Layout & Spacing)
- **栅格系统**: 24栅格
- **容器间距**: 20px (Dashboard页面)
- **组件间距**:
    - 小 (xs): 4px
    - 小 (sm): 8px (图标与文字)
    - 中 (md): 16px (表单项之间)
    - 大 (lg): 24px (区块之间)
    - 特大 (xl): 32px (页面区块)

---

## 🧩 详细组件配色规范 (Detailed Component Specifications)

### 一、基础交互组件 (Basic Interaction)

#### 1. 按钮 (Button)

**主按钮 (Primary)**
```css
.el-button--primary {
  background-color: var(--el-color-primary);
  border-color: var(--el-color-primary);
  color: #FFFFFF;
  box-shadow: 0 2px 6px rgba(64, 81, 181, 0.2);
}
.el-button--primary:hover {
  background-color: var(--el-color-primary-dark-2);
  border-color: var(--el-color-primary-dark-2);
}
```

**品牌按钮 (Accent)**
```css
.el-button--warning {  /* 使用 warning 类型代表品牌橙色 */
  background-color: var(--el-color-accent);
  border-color: var(--el-color-accent);
  color: #FFFFFF;
}
```

**次要按钮 (Default)**
```css
.el-button--default {
  background-color: #FFFFFF;
  border-color: var(--el-border-color);
  color: var(--el-text-color-regular);
}
.el-button--default:hover {
  color: var(--el-color-primary);
  border-color: var(--el-color-primary-light-7);
  background-color: var(--el-color-primary-light-9);
}
```

**文字按钮 (Text)**
```css
.el-button--text {
  border: none;
  color: var(--el-color-primary);
  background: transparent;
}
```

**规则**:
- 主按钮用于核心操作（保存、提交、新建）
- 品牌按钮用于高亮引导（升级VIP、推广）
- 危险按钮用红色，仅用于删除操作
- 图标按钮必须配合 Tooltip

---

#### 2. 输入框 (Input)

**默认状态**
```css
.el-input__wrapper {
  background-color: #FFFFFF;
  border: 1px solid var(--el-border-color);
  border-radius: var(--el-border-radius-base);
}
```

**聚焦状态**
```css
.el-input__wrapper.is-focus {
  border-color: var(--el-color-primary);
  box-shadow: 0 0 0 2px rgba(64, 81, 181, 0.15);
}
```

**禁用状态**
```css
.el-input.is-disabled .el-input__wrapper {
  background-color: var(--el-fill-color-light);
  color: var(--el-text-color-placeholder);
  cursor: not-allowed;
}
```

**规则**:
- 必填项 Label 右侧显示红色 `*`
- 搜索框必须开启 `clearable`
- 错误状态边框变红

---

#### 3. 选择器 (Select / Cascader)

**下拉面板**
```css
.el-select-dropdown {
  background: #FFFFFF;
  border: 1px solid var(--el-border-color-light);
  box-shadow: var(--el-box-shadow-light);
}
```

**选项悬停**
```css
.el-select-dropdown__item:hover {
  background-color: var(--el-fill-color-light);
}
.el-select-dropdown__item.selected {
  color: var(--el-color-primary);
  font-weight: 600;
}
```

**规则**:
- 宽度默认 100% (表单) 或固定宽度 (搜索栏)
- 选项过长需省略号 + Tooltip
- 多选模式使用 Tag 展示

---

#### 4. 开关 (Switch)

**激活状态**
```css
.el-switch.is-checked .el-switch__core {
  background-color: var(--el-color-primary);
  border-color: var(--el-color-primary);
}
```

**未激活状态**
```css
.el-switch__core {
  background-color: var(--el-border-color);
}
```

---

#### 5. 单选框 / 复选框 (Radio / Checkbox)

**选中状态**
```css
.el-radio__input.is-checked .el-radio__inner,
.el-checkbox__input.is-checked .el-checkbox__inner {
  background-color: var(--el-color-primary);
  border-color: var(--el-color-primary);
}
```

**规则**:
- 选项组垂直排列时间距 12px
- 水平排列时间距 24px

---

### 二、数据展示组件 (Data Display)

#### 6. 表格 (Table)

**表头**
```css
.el-table thead {
  background-color: #F5F7FA;
  color: var(--el-text-color-regular);
  font-weight: 600;
}
.el-table th {
  height: 48px;
  border-bottom: 1px solid var(--el-border-color);
}
```

**表格行**
```css
.el-table tbody tr {
  height: 48px;
}
.el-table tbody tr:hover {
  background-color: var(--el-fill-color-lighter);
}
```

**斑马纹**
```css
.el-table--striped .el-table__body tr:nth-child(2n) {
  background-color: #FAFAFA;
}
```

**规则**:
- 文本左对齐，数字右对齐，操作列居中
- 必须显示分页器（除非少量静态数据）
- 树形表格层级缩进 24px

---

#### 7. 树 (Tree)

**节点选中**
```css
.el-tree-node.is-current > .el-tree-node__content {
  background-color: var(--el-color-primary-light-9);
  border-left: 3px solid var(--el-color-primary);
}
```

**节点悬停**
```css
.el-tree-node__content:hover {
  background-color: var(--el-fill-color-light);
}
```

**规则**:
- 文件夹图标与文件图标区分
- 悬停节点时右侧显示操作按钮

---

#### 8. 标签 (Tag)

**状态色映射**
```css
.el-tag--success { background-color: var(--el-color-success-light); color: var(--el-color-success); }
.el-tag--warning { background-color: var(--el-color-warning-light); color: var(--el-color-warning); }
.el-tag--danger { background-color: var(--el-color-danger-light); color: var(--el-color-danger); }
.el-tag--info { background-color: var(--el-color-info-light); color: var(--el-color-info); }
```

**规则**:
- 成功/启用: Success (绿)
- 失败/禁用: Danger (红)
- 进行中: Primary (蓝)
- 警告/异常: Warning (橙)
- 未知/草稿: Info (灰)

---

#### 9. 徽标数 (Badge)

**红点提示**
```css
.el-badge__content {
  background-color: var(--el-color-danger);
  color: #FFFFFF;
  border-radius: 10px;
}
```

---

#### 10. 头像 (Avatar)

**默认背景**
```css
.el-avatar {
  background-color: var(--el-fill-color-light);
  color: var(--el-text-color-secondary);
}
```

---

#### 11. 时间轴 (Timeline)

**节点颜色**
```css
.el-timeline-item__node {
  background-color: var(--el-color-primary);
}
.el-timeline-item__node--success {
  background-color: var(--el-color-success);
}
```

---

#### 12. 走马灯 (Carousel)

**指示器**
```css
.el-carousel__indicator button {
  background-color: rgba(0, 0, 0, 0.2);
}
.el-carousel__indicator.is-active button {
  background-color: var(--el-color-primary);
}
```

---

### 三、导航组件 (Navigation)

#### 13. 标签页 (Tabs)

**卡片式**
```css
.el-tabs--card > .el-tabs__header .el-tabs__item {
  border: 1px solid var(--el-border-color);
  background-color: var(--el-fill-color-light);
}
.el-tabs--card > .el-tabs__header .el-tabs__item.is-active {
  background-color: #FFFFFF;
  border-bottom-color: #FFFFFF;
  color: var(--el-color-primary);
}
```

**下划线式**
```css
.el-tabs__active-bar {
  background-color: var(--el-color-primary);
  height: 2px;
}
.el-tabs__item.is-active {
  color: var(--el-color-primary);
}
```

**规则**:
- 页面级导航用卡片式
- 详情页内用下划线式
- 超过 8 个显示滚动箭头

---

#### 14. 面包屑 (Breadcrumb)

```css
.el-breadcrumb__item:last-child .el-breadcrumb__inner {
  color: var(--el-text-color-regular);
}
.el-breadcrumb__item .el-breadcrumb__inner:hover {
  color: var(--el-color-primary);
}
```

---

#### 15. 菜单 (Menu)

**侧边栏菜单**
```css
.el-menu {
  background-color: #FFFFFF;
  border-right: 1px solid var(--el-border-color);
}
.el-menu-item:hover {
  background-color: var(--el-color-primary-light-9);
}
.el-menu-item.is-active {
  background-color: var(--el-color-primary-light-9);
  color: var(--el-color-primary);
  border-right: 3px solid var(--el-color-primary);
}
```

---

#### 16. 步骤条 (Steps)

**已完成步骤**
```css
.el-step__head.is-finish {
  color: var(--el-color-primary);
  border-color: var(--el-color-primary);
}
.el-step__title.is-finish {
  color: var(--el-color-primary);
}
```

**当前步骤**
```css
.el-step__head.is-process {
  color: var(--el-color-primary);
  border-color: var(--el-color-primary);
  background-color: var(--el-color-primary);
}
```

---

#### 17. 分页 (Pagination)

```css
.el-pagination .el-pager li.active {
  background-color: var(--el-color-primary);
  color: #FFFFFF;
}
.el-pagination .el-pager li:hover {
  color: var(--el-color-primary);
}
```

---

### 四、反馈组件 (Feedback)

#### 18. 弹窗 (Dialog)

**遮罩层**
```css
.el-overlay {
  background-color: rgba(0, 0, 0, 0.5);
}
```

**弹窗容器**
```css
.el-dialog {
  background-color: #FFFFFF;
  border-radius: 8px;
  box-shadow: var(--el-box-shadow-dark);
}
.el-dialog__header {
  border-bottom: 1px solid var(--el-border-color-lighter);
  padding: 20px 24px;
}
.el-dialog__title {
  font-size: 18px;
  font-weight: 600;
  color: var(--el-text-color-primary);
}
```

**规则**:
- 标题左对齐
- 遮罩默认不可点击关闭 (`close-on-click-modal="false"`)
- 按钮底部右对齐，顺序 [取消] [确认]

---

#### 19. 抽屉 (Drawer)

```css
.el-drawer {
  background-color: #FFFFFF;
  box-shadow: var(--el-box-shadow-dark);
}
```

---

#### 20. 消息提示 (Message)

```css
.el-message--success {
  background-color: var(--el-color-success-light);
  border-color: var(--el-color-success);
}
.el-message--warning {
  background-color: var(--el-color-warning-light);
  border-color: var(--el-color-warning);
}
.el-message--error {
  background-color: var(--el-color-danger-light);
  border-color: var(--el-color-danger);
}
```

**规则**:
- 顶部居中显示
- 停留时间 3 秒
- 最多同时显示 3 条

---

#### 21. 通知 (Notification)

```css
.el-notification {
  background-color: #FFFFFF;
  border: 1px solid var(--el-border-color-lighter);
  box-shadow: var(--el-box-shadow);
}
```

**规则**:
- 右上角显示
- 用于系统级通知
- 可手动关闭

---

#### 22. 警告 (Alert)

```css
.el-alert--success {
  background-color: var(--el-color-success-light);
  color: var(--el-color-success);
}
.el-alert--warning {
  background-color: var(--el-color-warning-light);
  color: var(--el-color-warning);
}
.el-alert--error {
  background-color: var(--el-color-danger-light);
  color: var(--el-color-danger);
}
```

**规则**:
- 用于页面内常驻警告
- 可关闭或常驻

---

#### 23. 加载中 (Loading)

```css
.el-loading-mask {
  background-color: rgba(255, 255, 255, 0.9);
}
.el-loading-spinner .circular {
  stroke: var(--el-color-primary);
}
```

---

#### 24. 进度条 (Progress)

**线性进度条**
```css
.el-progress-bar__outer {
  background-color: var(--el-fill-color-light);
}
.el-progress-bar__inner {
  background-color: var(--el-color-primary);
}
```

**环形进度条**
```css
.el-progress__text {
  color: var(--el-text-color-primary);
}
```

---

#### 25. 骨架屏 (Skeleton)

```css
.el-skeleton__item {
  background: linear-gradient(
    90deg,
    var(--el-fill-color-lighter) 25%,
    var(--el-fill-color-light) 37%,
    var(--el-fill-color-lighter) 63%
  );
}
```

---

#### 26. 气泡提示 (Tooltip / Popover)

```css
.el-tooltip__popper,
.el-popover {
  background-color: #303133;
  color: #FFFFFF;
  border: 1px solid #303133;
  box-shadow: var(--el-box-shadow-light);
}
```

---

### 五、容器组件 (Container)

#### 27. 卡片 (Card)

```css
.el-card {
  background-color: #FFFFFF;
  border: 1px solid var(--el-border-color-lighter);
  border-radius: 8px;
  box-shadow: var(--el-box-shadow);
}
.el-card__header {
  border-bottom: 1px solid var(--el-border-color-lighter);
  padding: 18px 20px;
  font-weight: 600;
  color: var(--el-text-color-primary);
}
```

---

#### 28. 折叠面板 (Collapse)

```css
.el-collapse-item__header {
  background-color: var(--el-fill-color-lighter);
  border-bottom: 1px solid var(--el-border-color);
  color: var(--el-text-color-primary);
}
.el-collapse-item__header.is-active {
  color: var(--el-color-primary);
}
```

---

### 六、其他组件

#### 29. 上传 (Upload)

**拖拽区域**
```css
.el-upload-dragger {
  background-color: #FFFFFF;
  border: 1px dashed var(--el-border-color);
}
.el-upload-dragger:hover {
  border-color: var(--el-color-primary);
}
```

---

#### 30. 穿梭框 (Transfer)

```css
.el-transfer-panel {
  border: 1px solid var(--el-border-color);
  background-color: #FFFFFF;
}
.el-transfer-panel__item.is-checked {
  background-color: var(--el-color-primary-light-9);
  color: var(--el-color-primary);
}
```

---

## 📐 页面布局模式 (Page Layout Patterns)

### 1. 搜索列表页 (List Page)
```
[面包屑 Breadcrumb]
[搜索栏 Card - 背景 #FFFFFF]
[工具栏 - 新建按钮 Primary]
[表格 Card - 表头 #F5F7FA]
[分页器 - 右下角]
```

### 2. 表单详情页 (Form Page)
```
[返回头 PageHeader]
[基础信息 Card]
[详细配置 Card]
[底部悬浮按钮栏 - 保存(Primary) 取消(Default)]
```

### 3. 仪表盘页 (Dashboard)
```
[统计卡片组 - 4列栅格]
[图表区域 - 2列栅格]
[数据表格 Card]
```

---

## 🚫 设计禁忌 (Design Don'ts)

1. **禁止**在业务代码中硬编码颜色值（如 `#4051B5`），必须使用 `var(--el-color-primary)`
2. **禁止**使用纯黑 `#000000` 作为文字颜色，最深使用 `#303133`
3. **禁止**表格无分页（除非是极少量静态数据 < 20 条）
4. **禁止**弹窗套弹窗（最多 2 层，再多用 Drawer）
5. **禁止**混乱的 Z-Index 管理，统一使用 Element Plus 的层级系统
6. **禁止**自定义滚动条样式（保持系统原生）
7. **禁止**过度动画（transition 不超过 300ms）

---

## ✅ 最佳实践 (Best Practices)

1. **优先使用 Element Plus 组件**，避免重复造轮子
2. **所有颜色通过 CSS 变量引用**，便于未来主题切换
3. **表单必须有验证规则**，错误提示清晰
4. **空状态使用 Empty 组件**，提供友好提示
5. **加载状态使用 Skeleton 或 Loading**，提升用户体验
6. **复杂操作必须二次确认**，使用 Popconfirm 或 MessageBox
7. **图标与文字间距 8px**，保持视觉平衡

---

**文档维护**: 本规范为项目前端开发的唯一标准，所有组件样式必须严格遵循。如需调整，请更新本文档并通知团队。
